这个文件夹下是一些关于Mach ipc的例子。
    bootstrap_ipc.c、receive_integer.c和send_intenger.c是一个例子，boot.c是一个完整的程序，可以单独运行。receive和send是接收和发送消息的函数，boot.c中已经包括了后两者。
    ipc_hello.c同样是一个ipc的例子。
    ipc_between_task.c也是ipc的例子。